<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ImportantPeople" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_grey_light</item>
        <item name="colorPrimaryVariant">@color/dark_grey</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="textAllCaps">false</item>
        <item name="android:windowBackground">@color/dark_grey</item>
        <item name="cardViewStyle">@style/Theme.ImportantPeople.cardViewStyle</item>
    </style>

    <style name="Theme.ImportantPeople.ShapeImageView.Circle" parent="">
        <item name="cornerSize">50%</item>
        <item name="background">@android:color/transparent</item>
    </style>

    <style name="Theme.ImportantPeople.cardViewStyle">
        <item name="cardBackgroundColor">@color/dark_grey_light</item>
        <item name="cardCornerRadius">20dp</item>
    </style>
</resources>